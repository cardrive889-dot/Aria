# Aria — Better than the leaked Claude Code (2026)

Open-source AI terminal agent built from the leaked Claude Code architecture.

### Features (exactly like the leak + better)
- Living Buddy pet (Tamagotchi)
- Kairos proactive daemon
- autoDream self-healing memory
- Real local LLM (llama3.2:1b — runs on low RAM)
- 10+ tools (`/tool ls`, `/tool git`, `/tool echo`, etc.)
- Full slash commands

### How to run (forever)
1. Open Command Prompt
2. `cd C:\Users\Azhan\Desktop\Aria\Aria`
3. `python entrypoints\main.py start`

Press Ctrl+C to exit.

Enjoy your own private AI agent! 🚀
Made with love from the leaked source — now 100% yours.